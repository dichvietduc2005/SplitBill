ktor {
    deployment {
        host = 0.0.0.0
        port = 8081
        port = ${?PORT}
    }
    application {
        modules = [ com.splitbill.ApplicationKt.module ]
    }
}

jwt {
    secret = "splitbill-dev-secret-change-in-production"
    secret = ${?JWT_SECRET}
    issuer = "splitbill-server"
    validityDays = 7
    validityDays = ${?JWT_VALIDITY_DAYS}
}
